import{activeBroadcastRun,getBroadcastPlaylist,listBroadcastItems,markBroadcastItem,setArticleStatus,setBroadcastPlaylistState,setPlaybackState,tryStartBroadcastRun,updateBroadcastRun}from'@ans/database';import type{ObsController}from'@ans/obs-controller';
export type Control='pause'|'resume'|'skip'|'stop';
export interface BroadcastRunnerOptions{obs:ObsController;playlistId:string;overlayUrl:string;maintenanceDelayMs?:number;pollMs?:number;recoverRunId?:string;}
class ControlledStop extends Error{constructor(public finalStatus:'ended'|'interrupted'= 'ended'){super('Sendelauf kontrolliert beendet');}}
export class BroadcastRunner{private controls:Control[]=[];private running=false;constructor(private opts:BroadcastRunnerOptions){}isRunning(){return this.running;}control(c:Control){this.controls.push(c);}private take(c:Control){const i=this.controls.indexOf(c);if(i<0)return false;this.controls.splice(i,1);return true;}private has(c:Control){return this.controls.includes(c);}async start(){if(this.running)throw new Error('Sendelauf läuft bereits in diesem Prozess');let run=this.opts.recoverRunId?await activeBroadcastRun():null;if(!run){const existing=await activeBroadcastRun();if(existing)throw new Error('Es läuft bereits ein aktiver Sendelauf');run=await tryStartBroadcastRun(this.opts.playlistId);}if(!run)throw new Error('Aktiver Sendelauf konnte nicht gestartet werden');this.running=true;await setBroadcastPlaylistState(this.opts.playlistId,'running');try{await this.loop(run.id);await updateBroadcastRun(run.id,'ended',{status:'ended'});await setBroadcastPlaylistState(this.opts.playlistId,'ended');await setPlaybackState({status:'idle'});}catch(e){if(e instanceof ControlledStop){await updateBroadcastRun(run.id,e.finalStatus,{status:e.finalStatus,reason:e.message});await setBroadcastPlaylistState(this.opts.playlistId,e.finalStatus==='ended'?'ended':'error');await setPlaybackState({status:e.finalStatus});return;}const error=e instanceof Error?e.message:String(e);await updateBroadcastRun(run.id,'error',{status:'error',error});await setBroadcastPlaylistState(this.opts.playlistId,'error');await setPlaybackState({status:'error',error});throw e;}finally{this.running=false;}}private async waitWhilePaused(runId:string,item?:{id:string;article_id:string},position?:number){if(!this.take('pause')&&!this.has('pause'))return;await setBroadcastPlaylistState(this.opts.playlistId,'paused');await updateBroadcastRun(runId,'paused',{status:'paused',itemId:item?.id,articleId:item?.article_id,position});await setPlaybackState({status:'paused',playlistId:this.opts.playlistId,runId,itemId:item?.id,articleId:item?.article_id,position});while(!this.take('resume')){if(this.take('stop'))throw new ControlledStop('interrupted');await new Promise(r=>setTimeout(r,this.opts.pollMs??100));}await updateBroadcastRun(runId,'running',{status:'running',itemId:item?.id,articleId:item?.article_id,position});await setBroadcastPlaylistState(this.opts.playlistId,'running');}private async loop(runId:string){const playlist=await getBroadcastPlaylist(this.opts.playlistId);if(!playlist)throw new Error('Sendeliste nicht gefunden');const items=await listBroadcastItems(playlist.id);for(let i=playlist.current_position;i<items.length;i++){const item=items[i];await this.waitWhilePaused(runId,item,i);if(this.take('stop'))break;if(this.take('skip')){await markBroadcastItem(item.id,'skipped','Manuell übersprungen');continue;}await setBroadcastPlaylistState(playlist.id,'running',i);try{if(!item.audio_path)throw new Error('Kein Sprecher-Audio für Beitrag vorhanden');await markBroadcastItem(item.id,'playing');await setArticleStatus(item.article_id,'published');const base={playlistId:playlist.id,runId,itemId:item.id,articleId:item.article_id,position:i};await updateBroadcastRun(runId,'running',{status:'preparing',...base});await setPlaybackState({status:'preparing',...base});await new Promise(r=>setTimeout(r,this.opts.maintenanceDelayMs??250));await this.opts.obs.playTestContribution({articleId:item.article_id,audioPath:item.audio_path,overlayUrl:this.opts.overlayUrl,onState:async s=>{await updateBroadcastRun(runId,s.status==='paused'?'paused':'running',{...s,...base});await setPlaybackState({...s,...base});},control:async()=>{if(this.take('stop'))return 'stop';if(this.take('skip'))return 'skip';if(this.take('pause'))return 'pause';return undefined;},onPaused:()=>this.waitWhilePaused(runId,item,i)});await markBroadcastItem(item.id,'played');}catch(e){if(e instanceof Error&&e.message==='skip'){await markBroadcastItem(item.id,'skipped','Manuell übersprungen');continue;}if(e instanceof Error&&e.message==='stop')throw new ControlledStop('interrupted');const error=e instanceof Error?e.message:String(e);await markBroadcastItem(item.id,'error',error);await setPlaybackState({status:'error',playlistId:playlist.id,runId,itemId:item.id,articleId:item.article_id,position:i,error});continue;}}}}
export function startInBackground(runner:BroadcastRunner){void runner.start().catch(()=>undefined);return runner;}
