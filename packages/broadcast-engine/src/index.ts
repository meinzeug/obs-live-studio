import{activeBroadcastRun,getBroadcastPlaylist,listBroadcastItems,markBroadcastItem,setArticleStatus,setBroadcastPlaylistState,setPlaybackState,tryStartBroadcastRun,updateBroadcastRun}from'@ans/database';import type{ObsController}from'@ans/obs-controller';
export type Control='pause'|'resume'|'skip'|'stop';
export interface BroadcastRunnerOptions{obs:ObsController;playlistId:string;overlayUrl:string;maintenanceDelayMs?:number;pollMs?:number;}
export class BroadcastRunner{private controls:Control[]=[];private running=false;constructor(private opts:BroadcastRunnerOptions){}isRunning(){return this.running;}control(c:Control){this.controls.push(c);}private take(c:Control){const i=this.controls.indexOf(c);if(i<0)return false;this.controls.splice(i,1);return true;}async start(){if(this.running)throw new Error('Sendelauf läuft bereits in diesem Prozess');const existing=await activeBroadcastRun();if(existing)throw new Error('Es läuft bereits ein aktiver Sendelauf');const run=await tryStartBroadcastRun(this.opts.playlistId);if(!run)throw new Error('Aktiver Sendelauf konnte nicht gestartet werden');this.running=true;await setBroadcastPlaylistState(this.opts.playlistId,'running',0);try{await this.loop(run.id);await updateBroadcastRun(run.id,'ended',{status:'ended'});await setBroadcastPlaylistState(this.opts.playlistId,'ended');await setPlaybackState({status:'idle'});}catch(e){const error=e instanceof Error?e.message:String(e);await updateBroadcastRun(run.id,'error',{status:'error',error});await setBroadcastPlaylistState(this.opts.playlistId,'error');await setPlaybackState({status:'error',error});throw e;}finally{this.running=false;}}private async waitWhilePaused(){while(this.take('pause')||this.controls.includes('pause')){await setBroadcastPlaylistState(this.opts.playlistId,'paused');await setPlaybackState({status:'paused'});while(!this.take('resume')){if(this.take('stop'))throw new Error('Sendelauf kontrolliert beendet');await new Promise(r=>setTimeout(r,this.opts.pollMs??250));}await setBroadcastPlaylistState(this.opts.playlistId,'running');}}private async loop(runId:string){const playlist=await getBroadcastPlaylist(this.opts.playlistId);if(!playlist)throw new Error('Sendeliste nicht gefunden');const items=await listBroadcastItems(playlist.id);for(let i=playlist.current_position;i<items.length;i++){await this.waitWhilePaused();if(this.take('stop'))break;if(this.take('skip')){await markBroadcastItem(items[i].id,'skipped','Manuell übersprungen');continue;}await setBroadcastPlaylistState(playlist.id,'running',i);const item=items[i];try{if(!item.audio_path)throw new Error('Kein Sprecher-Audio für Beitrag vorhanden');await markBroadcastItem(item.id,'playing');await setArticleStatus(item.article_id,'published');await setPlaybackState({status:'preparing',playlistId:playlist.id,runId,itemId:item.id,articleId:item.article_id,position:i});await new Promise(r=>setTimeout(r,this.opts.maintenanceDelayMs??250));await this.opts.obs.playTestContribution({articleId:item.article_id,audioPath:item.audio_path,overlayUrl:this.opts.overlayUrl,onState:s=>setPlaybackState({...s,playlistId:playlist.id,runId,itemId:item.id,position:i})});await markBroadcastItem(item.id,'played');}catch(e){const error=e instanceof Error?e.message:String(e);await markBroadcastItem(item.id,'error',error);await setPlaybackState({status:'error',playlistId:playlist.id,runId,itemId:item.id,articleId:item.article_id,position:i,error});continue;}}}
}
export function startInBackground(runner:BroadcastRunner){void runner.start().catch(()=>undefined);return runner;}
