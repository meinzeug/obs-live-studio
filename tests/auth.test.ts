import{describe,it,expect}from'vitest';import{hashPassword,verifyPassword,ROLE_PERMISSIONS,isWriteMethod,safeEqual}from'../packages/security/src/auth.js';
describe('authentication primitives',()=>{it('uses argon2id hashes and verifies passwords',async()=>{const hash=await hashPassword('correct horse battery staple');expect(hash).toContain('argon2id');await expect(verifyPassword(hash,'correct horse battery staple')).resolves.toBe(true);await expect(verifyPassword(hash,'wrong')).resolves.toBe(false);});it('maps write permissions by role and detects unsafe methods',()=>{expect(ROLE_PERMISSIONS.administrator).toContain('users:write');expect(ROLE_PERMISSIONS.redaktion).toContain('articles:write');expect(ROLE_PERMISSIONS.nur_lesen).toHaveLength(0);expect(isWriteMethod('POST')).toBe(true);expect(isWriteMethod('GET')).toBe(false);expect(safeEqual('abc','abc')).toBe(true);expect(safeEqual('abc','abd')).toBe(false);});});
