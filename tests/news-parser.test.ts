import{describe,it,expect}from'vitest';import{parseFeed,parseHtmlArticle,contentHash}from'../packages/news-parser/src/index.js';import{summarize,makeScript,classifyCritical}from'../packages/content-processing/src/index.js';
describe('news pipeline',()=>{it('parses rss and creates editorial text',()=>{const feed='<rss><channel><item><title>Demo Meldung</title><link>https://example.org/a</link><description>Dies ist ein längerer sachlicher Meldungstext. Er enthält ausreichend Informationen für eine kurze Zusammenfassung.</description></item></channel></rss>';const [a]=parseFeed(feed,'https://example.org');expect(a.title).toBe('Demo Meldung');const s=summarize(a.text);expect(makeScript(a.title,s,'Demoquelle')).toContain('Nach Angaben');});it('extracts html content and hash',()=>{const a=parseHtmlArticle('<article><h1>Titel</h1><p>Ein langer Absatz mit Nachrichtentext für die Extraktion.</p></article>','https://example.org');expect(a.text).toContain('Nachrichtentext');expect(contentHash(a.text)).toHaveLength(64);});it('marks critical content for review',()=>{expect(classifyCritical('Die Polizei meldet einen Einsatz.').length).toBeGreaterThan(0);});});
