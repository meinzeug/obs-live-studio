import{describe,it,expect}from'vitest';
import{ObsController,MAIN_NEWS_SCENE,MAINTENANCE_SCENE,MAIN_BROWSER_INPUT,VOICE_INPUT}from'@ans/obs-controller';

class MockObs{calls:{type:string;data?:Record<string,unknown>}[]=[];mediaPolls=0;async connect(){return{}}async disconnect(){}async call(type:string,data?:Record<string,unknown>){this.calls.push({type,data});if(type==='GetSceneList')return{scenes:[]};if(type==='GetInputList')return{inputs:[]};if(type==='GetMediaInputStatus')return{mediaState:++this.mediaPolls>1?'OBS_MEDIA_STATE_ENDED':'OBS_MEDIA_STATE_PLAYING'};return{};}}

describe('OBS controller v5 workflow',()=>{it('creates documented scenes and plays a test contribution',async()=>{const client=new MockObs();const states:string[]=[];const obs=new ObsController({host:'127.0.0.1',port:4455,client});await obs.playTestContribution({articleId:'a',audioPath:'/tmp/voice.wav',overlayUrl:'http://127.0.0.1:12000/overlay/main',timeoutMs:3000,onState:s=>{states.push(s.status)}});expect(client.calls.some(c=>c.type==='CreateScene'&&c.data?.sceneName===MAIN_NEWS_SCENE)).toBe(true);expect(client.calls.some(c=>c.type==='CreateScene'&&c.data?.sceneName===MAINTENANCE_SCENE)).toBe(true);expect(client.calls.some(c=>c.type==='CreateInput'&&c.data?.inputName===MAIN_BROWSER_INPUT&&c.data?.inputKind==='browser_source')).toBe(true);expect(client.calls.some(c=>c.type==='CreateInput'&&c.data?.inputName===VOICE_INPUT&&c.data?.inputKind==='ffmpeg_source')).toBe(true);expect(client.calls.some(c=>c.type==='TriggerMediaInputAction')).toBe(true);expect(client.calls.at(-1)?.data?.sceneName).toBe(MAINTENANCE_SCENE);expect(states).toEqual(['preparing','playing','ended']);});});
