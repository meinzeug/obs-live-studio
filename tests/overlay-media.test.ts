import{describe,expect,it}from'vitest';import{createTemplate,renderOverlay,validateOverlayDocument}from'../packages/overlay-engine/src/index.js';import{inspectImage}from'../packages/media-engine/src/index.js';
describe('overlay engine validation',()=>{it('creates safe main-news documents and binds text without html interpolation',()=>{const doc=createTemplate('main-news',1920,1080);const rendered=renderOverlay(doc,{article:{title:'<b>Alarm</b>',summary:'Zusammenfassung',source:'Quelle'},serverTime:'2026-07-12T10:00:00Z'});expect(validateOverlayDocument(doc).elements.length).toBeGreaterThan(0);expect(rendered.find(e=>e.type==='text')?.text).toContain('<b>Alarm</b>');expect(rendered.find(e=>e.type==='text')?.style.boxSizing).toBe('border-box');});it('rejects unsupported element properties',()=>{const doc=createTemplate('ticker',1920,1080) as any;doc.elements[0].props.color='url(javascript:alert(1))';expect(()=>validateOverlayDocument(doc)).toThrow();});});
describe('media inspection',()=>{it('accepts real png signatures and rejects mime confusion',()=>{const png=Buffer.alloc(33);Buffer.from([0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a]).copy(png,0);png.writeUInt32BE(1,16);png.writeUInt32BE(1,20);expect(inspectImage(png,'image/png').mime).toBe('image/png');expect(()=>inspectImage(png,'image/jpeg')).toThrow(/MIME/);});});
